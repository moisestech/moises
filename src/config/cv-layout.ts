/**
 * Fixed site header clearance for `/cv/*` (logo row + desktop nav).
 * Prefer `siteHeaderExpandedPaddingTopClass` from `site-header-layout` for new pages.
 */
export const CV_MAIN_PADDING_TOP = 'pt-28 sm:pt-32 md:pt-36 lg:pt-40';

/** Collapsed fixed header — prefer `siteHeaderStickyTopClass` from `site-header-layout`. */
export const CV_SUBNAV_STICKY_TOP = 'top-20';
