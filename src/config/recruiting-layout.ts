/**
 * Fixed recruiting header is two rows on md+ (logo + desktop links), one row on mobile.
 * Keep in sync with `RecruitingSiteHeader` vertical rhythm.
 */
export const RECRUITING_HEADER_OFFSET = '10.5rem';

/** Tailwind-friendly padding below fixed header + 150px margin */
export const RECRUITING_MAIN_PADDING_TOP = 'pt-[calc(10.5rem+150px)]';

