/**
 * Public `/forward-deployed` send is the Design Facilitator FDE interview dossier.
 * Lifecycle cases remain in `src/content/evidence/flagships.ts`.
 * Generic role overlay: `/opportunities/forward-deployed-ai-engineer`.
 */
export { forwardDeployedInterviewOpportunity as forwardDeployedFlagship } from '@/content/opportunities/deloitte-ai-design-facilitator-fde';
